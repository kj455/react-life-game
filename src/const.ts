import { LifeGameOption } from './types';

export const defaultOption: LifeGameOption = {
  interval: 1000,
  cellSize: 12,
  initialAliveRatio: 0.1,
  aliveColor: '#1e3a8a',
  deadColor: '#0f172b',
};
